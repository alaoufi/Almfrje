# تقرير إصلاح مطابقة تحقّق دخول الزائر
# ALMFRJE_GUEST_VERIFY_MATCHING_FIX_REPORT

**المرحلة:** PHASE ALMFRJE-DATA-LOGIN-5 — FIX GUEST VERIFY API MATCHING
**المستودع:** `alaoufi/Almfrje` فقط
**الفرع:** `fix-almfrje-guest-verify-matching` (من `main`)
**التاريخ:** 2026-06-10

---

## 1) سبب المشكلة الحقيقي

رسالة «وُجد اسمك، لكن لم نجد آباءك/أجدادك» تظهر حين **لا يطابق أيُّ شخصٍ حيٍّ السلسلةَ كاملةً**
بينما يوجد اسمٌ أوّل حيٌّ مطابق. سبب ذلك أن دالة `lineageWords` (التي تبني سلسلة النسب بتتبّع
`father_id` عبر خريطة `byId` في ذاكرة الخادم) كانت **تنكسر** فلا تصل إلى الأب/الجدّ، رغم أن
الروابط في القاعدة صحيحة. وللانكسار سببان في الكود (لا في البيانات):

1. **تحميلٌ غير مرتّب للأشخاص (الأهمّ):** كان جلب الأشخاص يتم بصفحاتٍ عبر `.range(from, from+999)`
   **بلا `.order()`**. الترقيم في PostgREST/Postgres دون ترتيبٍ ثابت قد يُرجِع صفحاتٍ **متداخلة
   أو فيها فجوات** (عند تجاوز العدد 1000 شخص)، فيغيب بعض الأشخاص من المصفوفة المحمّلة. فإن غاب
   **الأب «محمد»** (أو الجدّ «شامان») من الذاكرة، فإن `byId.get(father_id)` يُرجِع `undefined`،
   فتتوقّف `lineageWords` عند «سامي» وحده → تفشل `matchSubseq` → تظهر الرسالة.

2. **نوع مفتاح المطابقة:** كانت `byId` بمفاتيح رقمية `p.id`. لو رجعت أعمدة `id`/`father_id`
   (وهي غالباً `int8`) **كنصوص** بدل أرقام، لاختلّ `byId.get(father_id)` بسبب اختلاف النوع
   (رقم مقابل نص) → نفس الانكسار.

## 2) لماذا كانت البيانات صحيحة لكن الدخول يفشل؟

لأن الخلل **ليس في البيانات** (روابط `father_id` لـ«سامي → محمد → شامان» صحيحة وحيّة في الإنتاج
كما تحقّقتَ)، بل في **إعادة بناء السلسلة داخل ذاكرة الخادم**: لم تكن كلُّ الأشخاص محمّلةً بشكلٍ
كامل/ثابت، فتعذّر على الخادم اتّباع الرابط `سامي → محمد → شامان` رغم وجوده في القاعدة. أي أن
الاتصال بالقاعدة سليم، والبيانات سليمة، لكنّ **خطوة التحميل/التتبّع في الكود** كانت هشّة.

> ولهذا **لا يتأثّر كل المستخدمين**: يفشل فقط من تقع سلسلةُ آبائه ضمن الصفوف المُتخطّاة في
> التحميل غير المرتّب — لا الجميع.

## 3) الملفات المعدَّلة
| الملف | التغيير |
|---|---|
| `app/api/almfrje-guest-verify/route.ts` | تحميلٌ مرتّب وكامل + بحثٌ آمن النوع في `byId` |
| `ALMFRJE_GUEST_VERIFY_MATCHING_FIX_REPORT.md` | هذا التقرير |

**لم يُعدَّل** `public/almfrje/app.js` (الواجهة)، ولا `config.js`، ولا أي مسارٍ آخر، ولا قاعدة البيانات.

## 4) ماذا تغيّر في المطابقة؟ (منطق المطابقة نفسه لم يتغيّر)

التغيير محصورٌ في **سلامة تحميل الأشخاص وبناء السلسلة**، لا في قواعد المطابقة (`matchSubseq` كما هي):

| السطر | قبل | بعد |
|---|---|---|
| تحميل الأشخاص | `.select(...).range(from, from+999)` | `.select(...).order('id', { ascending: true }).range(from, from+999)` |
| خريطة المعرّفات | `new Map<number,P>(persons.map(p => [p.id, p]))` | `new Map<string,P>(persons.map(p => [String(p.id), p]))` |
| تتبّع النسب (lineageWords) | `byId.get(cur)` | `byId.get(String(cur))` |
| بناء الاسم الكامل | `byId.get(cur)` | `byId.get(String(cur))` |

النتيجة: تُحمَّل **كل** الأشخاص بلا فجوات (ترتيب ثابت بالمعرّف)، ويُطابَق المفتاح **أرقاماً كان
أو نصوصاً**؛ فتُبنى سلسلة «سامي → محمد → شامان» كاملةً وتنجح `matchSubseq`. **رسائل الخطأ ومنطق
التفرّد (شخص حيّ واحد) لم تتغيّر.**

## 5) نتيجة اختبار «سامي محمد شامان»

- **اختبار التشغيل المباشر (POST):** تعذّر من هذه البيئة (أداة الجلب GET فقط، والـsandbox يحجب
  الخروج المباشر) — لم أتمكّن من إرسال POST فعلي للنقطة.
- **التحقّق المنطقي:** بما أن السجل موجود وحيٌّ وصحيح في الإنتاج (vzliv)، فبعد هذا الإصلاح
  ستُحمَّل «محمد» و«شامان» ضمن `byId` (تحميلٌ كامل ومرتّب)، فتُرجِع `lineageWords(سامي)` =
  `[سامي, محمد, شامان, …]`، فتُرجِع `matchSubseq` **صحيحاً**، فيقع «سامي» ضمن `liveMatches`
  → **دخولٌ ناجح** (أو رسالة «أضِف جدّاً» فقط إن وُجد أكثر من «سامي → محمد → شامان» حيّ — وهي
  سلوكٌ صحيحٌ قائم).
- **حالة البناء:** ناجحة (انظر §7).
- **للتأكيد النهائي:** بعد دمج هذا الـ PR ونشره، جرّب الدخول بـ«سامي محمد شامان» مع Hard Refresh.

## 6) خطة التراجع (Rollback)
1. **الأبسط:** إغلاق الـ Draft PR دون دمج.
2. **إن دُمج:** `git revert <commit_sha>` على فرعٍ جديد ثم PR — يعيد `route.ts` كما كان.
3. **يدوياً:** إزالة `.order('id', …)` وإرجاع `byId` لمفاتيح رقمية و`byId.get(cur)`. التغيير محصورٌ
   في ملفٍ واحد، بلا أثرٍ على قاعدة البيانات أو الواجهة.

## 7) حالة البناء
- `npm install` ✅
- `prebuild` (حارس الحدود) ✅ «لا تداخل بين almfrji / con / mrahi / notes».
- `npm run build` (Next.js 15.5.18) ✅ `✓ Compiled successfully` (الأنواع صحيحة)،
  `✓ Generating static pages (3/3)`، رمز الخروج `0`. لا سكربت `lint` مستقل.

> ملاحظة: لا يوجد إطار اختبارات في المشروع (لا سكربت `test`)، فلم أُضِف ملف اختبار حتى لا أضخّم
> المشروع؛ التحقّق يكون بالبناء + اختبار «سامي محمد شامان» الحيّ بعد النشر.

## 8) تأكيد عدم لمس قاعدة البيانات
✅ لم تُلمَس — لا `update/insert/delete/import`، لا migration، لا SQL. التعديل منطقُ تحميلٍ
وتتبّعٍ في كود الخادم فقط.

## 9) تأكيد عدم لمس Alaoufi.me أو marahi
✅ لم يُلمَس مستودع `alaoufi/Alaoufi.me` ولا `alaoufi/marahi` ولا PR #19. كل العمل داخل
`alaoufi/Almfrje` على فرع `fix-almfrje-guest-verify-matching` فقط.
