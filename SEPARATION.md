# عزل المشاريع الثلاثة — دليل الاستقلالية الكاملة

هذا المستودع يستضيف **ثلاثة تطبيقات منفصلة** على نشر Next.js واحد. هذا
المستند يشرح حدودها، نقاط التداخل الحالية، وكيفية فصلها فصلاً تامّاً بحيث
لا يؤثّر أيّ تطبيق على الآخر — لا في الكود ولا في البيانات ولا في الإعدادات.

## ١) خريطة التطبيقات

| التطبيق | الواجهة | مسارات API | المصادقة | جداول قاعدة البيانات |
|---|---|---|---|---|
| **المفرجي** (شجرة النسب) | `public/almfrje` | `app/api/almfrje-*` | Supabase Auth + RLS | `almfrje_*` |
| **الاستشارات** | `public/legacy` | `app/api/php/[[...path]]`, `app/api/migrate`, `app/api/setup`, `app/api/upload`, `app/api/ai/*` | JWT مخصّص (HS256) | `users`, `persons`, `appointments`, … (غير مبوّأة) |
| **مراح** | `public/mrahi` | `app/api/mrahi-config` | Supabase Auth + RLS | `mrahi_*` |

التوجيه بين الواجهات في `next.config.mjs` (rewrites): `/almfrje` و`/con` و`/mrah`.

## ٢) عزل الكود (منفّذ — لا تداخل)

- مسارات المفرجي التسعة **مكتفية ذاتياً**: كلٌّ ينشئ عميل Supabase خاصاً به
  داخلياً ولا يشارك حالة قابلة للتغيير مع غيره.
- `lib/db.ts` يخصّ **الاستشارات فقط** (عميل + JWT). لا يستورده أيّ مسار من
  مسارات المفرجي أو مراح.
- `lib/almfrje-schema.ts` يخصّ المفرجي فقط.
- مراح ويب يقرأ إعداده من `app/api/mrahi-config` فقط (anon + RLS، بلا مفتاح خدمي).

**القاعدة:** أيّ منطق مشترك جديد يجب ألّا يوضع في وحدة يستوردها أكثر من تطبيق.
إن لزم تشارك، انسخ أو ضَع الوحدة تحت مجلّد التطبيق الخاص به.

## ٣) عزل البيانات والإعدادات (يتطلّب إجراءً منك)

نقطة التداخل **الوحيدة** المتبقّية: عند غياب متغيّرات تطبيقٍ ما، يرجع تلقائياً
إلى قاعدة Supabase المشتركة (`NEXT_PUBLIC_SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY`
/ `NEXT_PUBLIC_SUPABASE_ANON_KEY`). فإن لم تُضبط متغيّرات الفصل، تتشارك التطبيقات
الثلاثة **مشروع Supabase واحداً** (ومفتاح خدمي واحداً) — يفصلها تسمية الجداول فقط.

### خطوات العزل الكامل

1. **أنشئ ثلاثة مشاريع Supabase منفصلة** (واحد لكل تطبيق).
2. **اضبط متغيّرات كل تطبيق** في Vercel (راجع `.env.example`، الأقسام ب/ج/د):
   - المفرجي: `ALMFRJE_SUPABASE_URL`, `ALMFRJE_SUPABASE_ANON_KEY`, `ALMFRJE_SERVICE_ROLE_KEY`, `ALMFRJE_SUPABASE_PAT`
   - الاستشارات: `CON_SUPABASE_URL`, `CON_SERVICE_ROLE_KEY`, `JWT_SECRET`, `SETUP_TOKEN`
   - مراح: `MRAHI_SUPABASE_URL`, `MRAHI_SUPABASE_ANON_KEY`
3. **أنشئ المخطط في كل قاعدة:**
   - المفرجي: يُنشأ تلقائياً (يستدعي `/api/almfrje-setup` عند أول فتح، باستخدام `ALMFRJE_SUPABASE_PAT`).
   - الاستشارات: شغّل `database/schema.sql` + `database/data.sql`.
   - مراح: شغّل `marahi-app/schema.sql`.
4. **انقل البيانات** (إن وُجدت) من القاعدة المشتركة إلى قاعدة كل تطبيق:
   - المفرجي: استخدم «النسخ والتصدير» داخل التطبيق (تصدير JSON من المشتركة ثم استعادة في قاعدة المفرجي).
   - الاستشارات/مراح: `pg_dump` للجداول المعنيّة ثم استيرادها في القاعدة الجديدة.
5. **أفرغ متغيّرات القاعدة المشتركة** (القسم أ) — أو خصّصها لتطبيقٍ واحدٍ فقط —
   حتى لا يبقى أيّ رجوعٍ صامت يربط التطبيقات.

> **توصية (الفرض الصارم):** لإلغاء الرجوع الصامت نهائياً يُنصح بإزالة احتياطي
> `|| NEXT_PUBLIC_SUPABASE_*` من مسارات كل تطبيق بحيث يفشل التطبيق صراحةً إن
> نقص متغيّره الخاص بدل أن يتشارك القاعدة المشتركة صامتاً. هذا تغييرٌ يكسر
> الإنتاج إن لم تُضبط المتغيّرات الخاصة أولاً، لذا يُطبَّق **بعد** إتمام الخطوات ١–٥.

## ٤) قائمة تحقّق العزل

- [ ] ثلاثة مشاريع Supabase منفصلة.
- [ ] متغيّرات كل تطبيق مضبوطة في Vercel.
- [ ] مفتاح خدمي (`service_role`) **مستقلّ** لكل قاعدة (لا مفتاح مشترك يصل لبيانات الجميع).
- [ ] مفتاح anon **مستقلّ** لكل تطبيق (anon المشترك يُمكّن قراءة `almfrje_settings` العامة من أي تطبيق).
- [ ] `JWT_SECRET` قيمة عشوائية قوية (وليست القيمة المسرّبة القديمة) — للاستشارات.
- [ ] متغيّرات القاعدة المشتركة (القسم أ) مفرّغة بعد النقل.
- [ ] التحقّق: تسجيل الدخول والقراءة/الكتابة تعمل في كل تطبيق على قاعدته.

## ٥) ملاحظات أمنية مرتبطة بالعزل

- **مفتاح خدمي مشترك** = أخطر نقطة: خللٌ في مسار خادمٍ لأي تطبيق يمنح وصولاً
  بصلاحية `service_role` لبيانات **كل** التطبيقات. العزل يحصر الضرر بتطبيقه.
- **anon مشترك**: سياسة `almfrje_settings` للقراءة عامة (`USING(true)` + `GRANT … TO anon`)،
  فأيّ حامل anon (حتى من تطبيق آخر) يقرأ إعدادات المفرجي العامة (لافتة/تعليمات/إحصاء زيارات).
  العزل بمفتاح anon مستقلّ يزيل هذا.
- **`JWT_SECRET` المسرّب** (الاستشارات): راجع المراجعة الأمنية — يجب تدويره فوراً.
