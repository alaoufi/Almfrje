# تقرير إصلاح دخول المفرجي بالاسم/النسب
# ALMFRJE_NAME_LOGIN_FIX_REPORT

**المرحلة:** PHASE ALMFRJE-DATA-LOGIN-1 — AUDIT NAME LOGIN DATA MATCHING
**المستودع:** `alaoufi/Almfrje` فقط
**الفرع:** `fix-almfrje-name-login-matching` (من `main`)
**التاريخ:** 2026-06-10

---

## 1) سبب المشكلة

الرسالة «وُجد اسمك، لكن لم نجد آباءك/أجدادك بما كتبت — تأكّد من ترتيب الأسماء» تصدر من
**الخادم** (`app/api/almfrje-guest-verify/route.ts:112`)، حين يجد الاسم الأول حيّاً لكن لا
تُطابِق سلسلةُ النسب أيَّ شخص. التشخيص:

- **منطق المطابقة سليم ولم يكسره تغيير المسارات (PR #2):** تحقّق دخول الزائر **خادميٌّ بالكامل**؛
  العميل يُرسل الإدخال الخام (`guestGateEnter`، app.js:4647) والخادم يطبّع ويطابق. وتطبيعُ
  الأسماء **متطابق** بين العميل (`normalizeAr`) والخادم (`normWord`). ومسارُ التحقّق هذا
  **لم يتغيّر** بتعديل خدمة الجذر (PR #2 لم يمسّ المسار)، ولا بـ `c0b2d26` (الذي بدّل قراءة
  البيئة إلى `almfrjeEnv()` المكافئة تماماً — «Behaviour unchanged»).

- **السبب المؤكَّد = تباين مشروع Supabase بين العرض والتحقّق:** الاحتياطيُّ الثابت
  `public/almfrje/config.js` كان يثبّت مشروعاً **قديماً** (`noupszhgfyqhfotokabj`)، بينما نقطةُ
  البيئة `/almfrje-config` — **ونفسُها مشروعُ تحقّق الخادم** عبر `almfrjeEnv()` — تستخدم
  مشروعاً **حالياً مختلفاً** (`vzlivdymyiaplnasplvg`، مُتحقَّقٌ منه حيّاً). في التشغيل العادي
  يَغلِب `/almfrje-config` على `config.js`، لكن حين يرجع العميل للاحتياطي الثابت (كاش/تعذُّر
  تحميل `/almfrje-config`) يتصفّح الزائر **شجرة المشروع القديم** ويُدخل أسماءً يتحقّق منها
  **الخادمُ في المشروع الحالي** → يُوجَد الاسم الأول ولا يُطابَق النسب → نفس الرسالة بالضبط.

## 2) هل كانت من config أو Supabase أو matching أو cache؟

- **config / Supabase:** نعم — تباينُ مشروعِ Supabase بين الاحتياطي الثابت (config.js) ونقطةِ
  البيئة/الخادم. هذا هو السبب الجذري المؤكَّد في الكود.
- **matching:** لا — منطق المطابقة سليم ومتطابق بين العميل والخادم، ولم يتغيّر.
- **cache:** عاملٌ مُحتمَل للتحفيز فقط (يجعل العميل يستخدم الاحتياطي القديم)؛ والإصلاح يُزيل
  الأثر بجعل الاحتياطي نفسه يطابق المشروع الصحيح. (لا يوجد service worker في المفرجي.)
- **تغيير المسارات (PR #2):** ليس السبب إطلاقاً.

## 3) الملفات التي فُحِصت

- `public/almfrje/app.js` — بوابة دخول الزائر (`guestGateEnter`)، `nameMatch`، `normalizeAr`،
  `lineage`، `configMissing`، تحميل الإعداد، وأماكن `location.*`.
- `public/almfrje/config.js` — الاحتياطي الثابت (المشروع المثبَّت).
- `app/almfrje-config/route.ts` — نقطة الإعداد الديناميكي (تُرجِع `almfrjeEnv().url/anon`).
- `app/api/almfrje-guest-verify/route.ts` — منطق التحقّق الخادمي ورسالة الخطأ.
- `lib/almfrje-env.ts` — مصدر إعداد قاعدة المفرجي (url/anon/service).
- `scripts/check-boundaries.mjs` — حارس prebuild.
- سجلّ Git للمسارات (لتحديد آخر تغيير: `c0b2d26`، وليس PR #2).
- نقطة `/almfrje-config` الحيّة (تأكيد المشروع الحالي = `vzliv…`).

## 4) الملفات التي عُدِّلت

| الملف | التغيير |
|---|---|
| `public/almfrje/config.js` | توحيد مشروع Supabase في الاحتياطي الثابت من القديم `noups…` إلى الحالي `vzliv…` (نفس مشروع البيئة وتحقّق الخادم) + توضيح في التعليق. |
| `ALMFRJE_NAME_LOGIN_FIX_REPORT.md` | هذا التقرير. |

**لم يُعدَّل أي منطق تطبيق** (لا app.js، لا مسارات API، لا قاعدة البيانات).

## 5) ماذا تغيّر بالضبط

في `public/almfrje/config.js` فقط:
```
- SUPABASE_URL:      https://noupszhgfyqhfotokabj.supabase.co   (مشروع قديم)
- SUPABASE_ANON_KEY: sb_publishable_MsV6jJsxCXDxcvrba4s9Jw_79t3eTVP
+ SUPABASE_URL:      https://vzlivdymyiaplnasplvg.supabase.co   (المشروع الحالي = نفس الخادم)
+ SUPABASE_ANON_KEY: sb_publishable_I7PYbWwN-KxV78vc1WDmpA_fnCEslM1
```
النتيجة: العميل (شجرة العرض) والخادم (تحقّق الدخول) يستخدمان **نفس المشروع دائماً** — سواءٌ عبر
`/almfrje-config` الديناميكي أو الاحتياطي الثابت — فلا يَفترق ما يراه الزائر عمّا يُطابِقه الخادم.

## 6) كيف تم اختبار الدخول

- **تحليل الكود الكامل لمسار الدخول:** العميل → `/api/almfrje-guest-verify` → التطبيع
  (`normWord`) → `matchSubseq` (الاسم ثم الأب ثم الجد بالترتيب) → فروع الرسائل (متوفّى/متكرّر/
  «وُجد اسمك…»). تأكّدتُ من تطابق التطبيع بين العميل والخادم حرفياً.
- **تأكيد مصدر البيانات حيّاً:** `/almfrje-config` يُرجِع `vzliv…` (مشروع الخادم)، بينما كان
  `config.js` يثبّت `noups…` — تباينٌ مؤكَّد.
- **حدود الاختبار (بصراحة):** تعذّر اختبار الدخول حيّاً بإدخال اسمٍ فعلي من هذه البيئة، لأن:
  (أ) أداة الجلب تدعم GET فقط فلا تُرسل POST لنقطة التحقّق؛ (ب) مشروعا Supabase للمفرجي
  (`vzliv`/`noups`) تحت **منظمةٍ أخرى غير متاحة** للأداة المتصلة، فلا يمكن الاستعلام/المقارنة في
  البيانات. لذا لم أتمكّن من إعادة إنتاج الحالة بالاسم المحدّد ولا من قياس البيانات.

> **مهم (وفق القاعدة 6):** إن استمرّت المشكلة بعد هذا الإصلاح **وتحديثٍ كامل للصفحة (Hard
> Refresh)**، فالسبب عندئذٍ في **البيانات نفسها** داخل مشروع `vzliv…` (نقصٌ/اختلافٌ في سلسلة
> آباء الشخص: مثلاً `father_id` مفقود أو يشير لشخصٍ ناقص). هذا **يُصحَّح في البيانات لا في الكود**،
> ولم أُجرِ أي تعديلٍ على قاعدة البيانات. يلزم حينها فحص سجلّ الشخص وآبائه في `vzliv`.

## 7) حالة البناء

- **`npm install`:** نجح ✅
- **`prebuild` (حارس الحدود):** اجتاز ✅ «لا تداخل بين almfrji / con / notes».
- **`npm run build`:** نجح ✅ — `next build` (Next.js 15.5.18): `✓ Compiled successfully in 3.5s`،
  فحص الأنواع/الـlint المدمج اجتاز، `✓ Generating static pages (3/3)`، رمز الخروج `0`.
- لا سكربت `lint` مستقل (الفحص ضمن `next build` + حارس الـ prebuild).

## 8) تأكيد عدم لمس قاعدة البيانات

تم. **لم تُلمَس قاعدة البيانات إطلاقاً** — لا migration، لا SQL، لا import، لا حذف، لا تعديل صفوف.
التغيير ضبطُ مشروعِ الاتصال في ملف إعداد الواجهة فقط.

## 9) تأكيد عدم لمس Alaoufi.me أو المشاريع الخارجية

تم. **لم يُلمَس مستودع `alaoufi/Alaoufi.me` ولا PR #19** — كل العمل داخل
`alaoufi/Almfrje` على فرع `fix-almfrje-name-login-matching` فقط.

## 10) خطة التراجع (Rollback)

1. **الأبسط:** إغلاق الـ Draft PR دون دمج.
2. **إن دُمج:** `git revert <commit_sha>` على فرعٍ جديد ثم PR — يعيد `config.js` إلى نسخته السابقة.
3. **يدوياً:** إرجاع قيمتَي `SUPABASE_URL`/`SUPABASE_ANON_KEY` في `public/almfrje/config.js`.
   التغيير محصورٌ في ملفٍ واحد، بلا أثرٍ على قاعدة البيانات أو منطق التطبيق.

---

### ملخّص الالتزام بالقيود
- ✅ تشخيصٌ قبل التعديل؛ لم أفترض السبب (أثبتُّ أن تغيير المسارات ليس السبب).
- ✅ إصلاحٌ بأقلّ تعديل (ملف إعداد واحد)؛ لا تغيير لمنطق التطبيق.
- ✅ لا تعديل لقاعدة البيانات، لا حذف بيانات، لا import.
- ✅ لم يُلمَس Alaoufi.me ولا PR #19؛ لا merge؛ لا force push؛ لا main مباشرة.
