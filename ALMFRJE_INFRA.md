# بنية تشغيل موقع قبيلة المفارجة — مرجع دائم (لا يحوي أسراراً)

> سُجّل هذا الملف بعد رحلة بحثٍ طويلة عن مكان قاعدة البيانات (٦ يوليو ٢٠٢٦) — اقرأه أولاً قبل أي عملٍ على البنية.

## 🗄️ قاعدة البيانات (الإنتاج) — Supabase
| البند | القيمة |
|---|---|
| مُعرّف المشروع (ref) | `noupszhgfyqhfotokabj` |
| الرابط | `https://noupszhgfyqhfotokabj.supabase.co` |
| **مكانه في اللوحة** | supabase.com ← مبدّل المنظمات ⇅ ← منظمة **«Almfrje»** (وليست «alaoufi.me»!) ← مشروع **«almfrje»** |
| محرّر SQL مباشرة | `https://supabase.com/dashboard/project/noupszhgfyqhfotokabj/sql/new` |
| المنطقة | AWS **ap-southeast-2 (سيدني)** — أُنشئ ~٦ يونيو ٢٠٢٦ |
| الخطة | مجانية (NANO) |

⚠️ **تنبيه من تجربة مريرة:** في حساب المالك منظمة أخرى «alaoufi.me» فيها مشروع باسم «alaoufi» (`vzlivdymyiaplnasplvg`) — هذا **نسخة قديمة مهجورة** من القاعدة (آخر بياناتها ٥ يونيو ٢٠٢٦، لا يوجد فيها جدول الملاحظات). أي SQL يُنفَّذ فيه **لا يصل الإنتاج**. لا تعمل عليه — عدا أن تخزينه (storage) ما يزال يستضيف صورة وثيقة اللزمة المشار إليها في الإعدادات.

## ☁️ الاستضافة — Vercel
- المشروع: `almfrje` (فريق المالك) مربوط بمستودع GitHub `alaoufi/Almfrje`، النشر تلقائي من فرع `main`.
- الدومين: `almfrje.alaoufi.me` (يُخدَم من جذر الدومين عبر rewrites في `next.config.mjs`).
- **منطقة الدوال: `syd1` (سيدني)** في `vercel.json` — لتطابق منطقة القاعدة. لا تغيّرها دون التحقق من منطقة القاعدة أولاً.
- وسيط قاعدة البيانات `/sbdb/:path*` (في `next.config.mjs`) يمرّر REST/Auth عبر نطاق الموقع — للشبكات التي تحجب نطاق Supabase (يتحوّل إليه المتصفح تلقائياً).

## 🔑 ترقية مخطط القاعدة (كيف تُطبَّق التعديلات)
المخطط الكامل في `lib/almfrje-schema.ts` (idempotent). مسار التطبيق `/api/almfrje-setup` يجرّب بالترتيب:
1. **Management API** بمفتاح `ALMFRJE_SUPABASE_PAT` (أو `SUPABASE_PAT`) — **غير مضاف حالياً في Vercel**.
2. **اتصال Postgres مباشر** عبر `ALMFRJE_DB_URL` (أو متغيرات POSTGRES_*) بشرط أن يحمل الرابط ref المشروع — **غير مضاف حالياً**.
3. عند فشل الاثنين: يظهر **توست ⚠️ للمدير** عند دخوله بنص السبب (لا فشل صامت).

**الوضع الحالي:** الترقيات تُطبَّق يدوياً بلصق SQL في محرّر المشروع (الرابط أعلاه) — تعمل لأن المالك يملك منظمة «Almfrje».
**التحسين المقترح:** إضافة `ALMFRJE_DB_URL` في Vercel (من Settings ← Database ← Connection string بعد حفظ كلمة سر القاعدة) فتصبح الترقيات تلقائية.

## 🧭 معالم أخرى
- مفاتيح البيئة في Vercel: `ALMFRJE_SUPABASE_URL / ALMFRJE_SUPABASE_ANON_KEY / ALMFRJE_SERVICE_ROLE_KEY` (والرجوع للمشتركة `NEXT_PUBLIC_*` عند غيابها) — انظر `lib/almfrje-env.ts`.
- الاحتياطي الثابت للمتصفح `public/almfrje/config.js` — **يجب أن يطابق مشروع الإنتاج دائماً** (اختلافه سابقاً سبّب ظهور شاشة المسؤول للزوار وفشل الدخول عند تعذر `/almfrje-config`).
- بريد المالك المعروف: alaoufi@gmail.com — لوحات Vercel وSupabase وGitHub كلها بحسابه.
- نسخ احتياطية: يومية سحابية داخل القاعدة + تنزيل يدوي (JSON كامل) من «النسخ والتصدير». آخر نسخة يدوية محفوظة عند المالك: ٦ يوليو ٢٠٢٦ (١٥٠٧ أشخاص).
